'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _propTypes = require('prop-types');

var _propTypes2 = _interopRequireDefault(_propTypes);

var _colors = require('./utils/colors');

var _colors2 = _interopRequireDefault(_colors);

require('../css/progressbar.css');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var defaultColor = _colors2.default.orangeLight;

var ProgressBar = function ProgressBar(_ref) {
  var color = _ref.color,
      value = _ref.value;

  var barColor = _colors2.default.array.indexOf(color) !== -1 ? color : defaultColor;

  var fixPercentage = function fixPercentage(percent) {
    if (percent < 0) {
      return 0;
    } else if (percent > 100) {
      return 100;
    }

    return percent;
  };

  var percentage = fixPercentage(value);
  var compensation = percentage < 10 ? 2 : 3;
  var percentagePadding = percentage < 2 ? 96 : fixPercentage(100 - percentage - compensation);

  return _react2.default.createElement(
    'div',
    { className: 'vn-progress-bar' },
    _react2.default.createElement('div', {
      className: 'vn-progress-bar__bar vn-background-color--' + barColor,
      style: { width: percentage + '%' }
    }),
    _react2.default.createElement('div', { className: 'vn-progress-bar__background-bar vn-background-color--gray' }),
    _react2.default.createElement(
      'span',
      {
        className: 'vn-progress-bar__percentage',
        style: { paddingRight: percentagePadding + '%' }
      },
      value + '%'
    )
  );
};

ProgressBar.propTypes = {
  color: _propTypes2.default.oneOf(_colors2.default.array),
  value: _propTypes2.default.number
};

ProgressBar.defaultProps = {
  color: defaultColor,
  value: 0
};

ProgressBar.displayName = 'ProgressBar';

exports.default = ProgressBar;
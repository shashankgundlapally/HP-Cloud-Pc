'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _propTypes = require('prop-types');

var _propTypes2 = _interopRequireDefault(_propTypes);

var _classnames = require('classnames');

var _classnames2 = _interopRequireDefault(_classnames);

require('../css/status_tracker.css');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var shortid = require('shortid');

var StatusTracker = function StatusTracker(_ref) {
  var current = _ref.current,
      items = _ref.items;
  return _react2.default.createElement(
    'div',
    { className: 'vn-status-tracker' },
    items.reduce(function (acc, item, index, array) {
      var currentIndex = array.indexOf(current);
      var dotClassName = (0, _classnames2.default)({
        'vn-status-tracker__step-dot': true,
        'vn-status-tracker__step-dot--active': index <= currentIndex
      });

      var labelClassName = (0, _classnames2.default)({
        'vn-status-tracker__step-label': true,
        'vn-status-tracker__step-label--active': index <= currentIndex
      });

      var barClassName = (0, _classnames2.default)({
        'vn-status-tracker__bar': true,
        'vn-status-tracker__bar--active': index < currentIndex
      });

      var isNotLast = index !== array.length - 1;

      var barCount = array.length - 1;
      // split the 100% width among each bar
      // and also split the 10px from the first dot left half
      // plus the 10px from the last dot right half
      var barWidth = 'calc(' + 100 / barCount + '% - ' + 20 / barCount + 'px)';

      var key = shortid.generate();

      acc.push(_react2.default.createElement(
        'div',
        { key: 'step' + key, className: 'vn-status-tracker__step' },
        _react2.default.createElement('div', { className: dotClassName }),
        _react2.default.createElement(
          'div',
          { className: labelClassName },
          item
        )
      ));
      if (isNotLast) {
        acc.push(_react2.default.createElement('div', { key: 'bar' + key, className: barClassName, style: { width: barWidth } }));
      }
      return acc;
    }, [])
  );
};

StatusTracker.propTypes = {
  current: _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.number]).isRequired,
  items: _propTypes2.default.arrayOf(_propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.number])).isRequired
};

StatusTracker.displayName = 'StatusTracker';

exports.default = StatusTracker;